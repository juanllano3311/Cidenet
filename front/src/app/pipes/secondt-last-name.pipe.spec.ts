import { SecondtLastNamePipe } from './secondt-last-name.pipe';

describe('SecondtLastNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondtLastNamePipe();
    expect(pipe).toBeTruthy();
  });
});
