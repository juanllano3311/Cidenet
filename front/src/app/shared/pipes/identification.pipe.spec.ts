import { IdentificationPipe } from './identification.pipe';

describe('IdentificationPipe', () => {
  it('create an instance', () => {
    const pipe = new IdentificationPipe();
    expect(pipe).toBeTruthy();
  });
});
