import { OthersNamesPipe } from './others-names.pipe';

describe('OthersNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new OthersNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
